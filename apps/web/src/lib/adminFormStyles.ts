export const BTN_PRIMARY = 'px-4 py-2 rounded-lg text-sm font-semibold bg-brand-400 text-stone-950 hover:bg-brand-300 disabled:opacity-50 transition-colors'
export const BTN_GHOST = 'px-4 py-2 rounded-lg text-sm font-medium bg-stone-700 text-stone-300 hover:bg-stone-600 transition-colors'

/** Input with `text-sm` — used in most edition/book admin forms. */
export const INP = 'w-full bg-stone-800 border border-stone-700 rounded-lg px-3 py-2 text-stone-100 focus:outline-none focus:border-brand-400 text-sm'

/** Label with `text-xs` — used in edition/book admin forms. */
export const LBL = 'block text-xs text-stone-400 mb-1'

/** Input without explicit `text-sm` — used in subscription / company admin pages. */
export const INPUT_CLASS =
  'w-full bg-stone-800 border border-stone-700 rounded-lg px-3 py-2 text-stone-100 focus:outline-none focus:border-brand-400'

/** Label with `text-sm` — used in subscription / company admin pages. */
export const LABEL_CLASS = 'block text-sm text-stone-400 mb-1'
